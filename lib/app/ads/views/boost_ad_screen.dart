import 'package:flutter/material.dart';

class BoostAdScreen extends StatelessWidget {
  const BoostAdScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Boost Ad Screen"),
      ),
    );
  }
}
